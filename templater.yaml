project:
  name: pico-template
  owner: ricardomgoncalves
  name_formalised: Template for pi pico project
  git:
    init: true
    folder_name: "{{ .project.name }}"
    submodules:
      "git@github.com:raspberrypi/pico-sdk.git": "external"
